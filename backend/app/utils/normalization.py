"""
Generic normalization helpers.

Responsibilities
----------------
- shared numeric normalization functions usable outside the
  scoring engine (e.g. min-max scaling, z-score helpers)
- kept separate from scoring.normalizer, which is scoring-specific

Sprint Status
-------------
Architecture phase only.
Implementation planned for Sprint 3.
"""
